import { EmptyState } from '@/components/shared/empty-state';

export default function DashboardPage() {
  return (
    <EmptyState
      eyebrow="Dashboard"
      title="Shell privada preparada"
      description="Aquí montaremos los primeros flujos funcionales una vez definamos los dominios de la app web."
    />
  );
}
